apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: broken-rs
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: apache   # ⚠ Mismatch
    spec:
      containers:
      - name: nginx
        image: nginx:1.25
